# Base URL of the admin API. Empty means "same origin", which is what nginx
# serves in production; the dev server proxies /api to the backend instead.
VITE_API_BASE_URL=

# Only used by `npm run dev`
VITE_DEV_API_TARGET=http://127.0.0.1:8000
